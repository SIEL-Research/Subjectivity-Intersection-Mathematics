# Change log -- full v0.92 to full v0.93

Date: 2026-09-26

## Retained without compression

- Preserved the complete 65-page v0.92 manuscript, including all v0.91 content plus the physical compiler, exploratory QPU execution, all-real-time hierarchical wave convergence, and the Higgs/Yukawa/fermion route through BGCE569.
- Preserved all prior public versions without overwrite.

## Joint neutral-matter action

- Added BGCE570's declared derived unital dagger-compact pointed-oriented Nambu--Lie completion.
- Added the exact joint-action relations `s(c-1)=0` and `s^2=c`; with `c=1`, the positive branch fixes `s=1` and excludes the separated-action `s=2` countermodel inside this class.
- Added the resulting `y_nu=1`, coefficient on the unnormalized `K=Y Y^T`, and six positive nondegenerate device-relative neutral modes.
- Preserved the boundary that this is not an unchanged-raw-source theorem, universal SI calibration, observed-neutrino naming, or empirical mass agreement.

## Source-order flavour mixing

- Added BGCE571's source-order numerical CKM matrix, unitary three-generation PMNS kernel, and physical `3x6` active--neutral mixing matrix without fitting observed data.
- Added the invariant magnitudes `|J_CKM|=0.028596762536667441` and `|J_PMNS^(3)|=0.0019627371637625296` and the `2.09e-15` row-isometry residual.
- Retained all six observed-name dictionaries as the finite orbit `{P^T V P : P in S3}`; no member is manually selected.
- Explicitly preserved measured-value agreement as an open empirical falsification task.

## Corrected boundaries

- Replaced the v0.92 statement that the dimensionful Majorana coefficient is open in all considered routes by the exact split: it is underdetermined for separated clock/pair data but fixed in the declared BGCE570 joint-action completion.
- Replaced “numerical CKM/PMNS open” by the exact split: source-order numerical matrices are derived, while observed-name assignment and measured-value agreement remain open.
- Kept the manuscript English-only; no Japanese abstract is embedded.
