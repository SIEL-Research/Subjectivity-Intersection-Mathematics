# Cellular return-identity interaction exploration

Correct headroom-normalized recovery exceeds mismatch in every scenario: `True`.
Correct recovery exceeds removal in every scenario: `True`.
Final correct-return specificity holds in every scenario: `True`.

This is result-informed local exploration in the frozen reduced E009 model.
