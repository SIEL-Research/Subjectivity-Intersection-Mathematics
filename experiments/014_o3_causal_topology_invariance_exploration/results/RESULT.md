# E014 local exploratory result

Decision: `PROSPECTIVE_CROSS_DOMAIN_O3_REENTRY_TEST_JUSTIFIED`

This is not a confirmatory result. It identifies a prospective testable claim
and preserves the exploratory path that produced it.

## Supported exploratory core

- Molecular full-fingerprint specificity: `1,347/1,347` dynamically
  distinguishable nonperiodic comparisons.
- Molecular exact grid covariance: `522/522`.
- Molecular removal-return four-edge topology: `14,210/14,210`.
- Atomic full-density removal-return topology: `50/50`.
- Cellular damage-dependent reinjection windows recovered with the unchanged
  E009 viability criterion.
- Domain-level directed recovery order: six of six marginal axes exact;
  label-permutation exceedances `0/65,536`.
- Initially selected domain readouts gave a positive correct-return identity by
  recovery-opportunity interaction in `2,043/2,043` groups, with all three
  leave-one-domain-out transfers passing.
- Molecular post-stabilization Hellinger specificity survived four removal
  durations, five late horizons, three grid factors, and all tested
  profile-temperature pairs (`10,440/10,440`).
- Atomic density specificity held at every tested return horizon; cellular
  specificity held for all explored conditions from 40 minutes after
  reinjection onward.
- Reserved cellular amplitudes `0.675` and `0.825` were not executed. Their
  immediate neighbours reproduced the same 45-minute-pass/50-minute-fail
  boundary in four independent cohorts and all `32/32` amplitude-time-cohort
  cells.
- The unnamed atomic mass/charge stress passed `120/120`; stabilized checks
  passed `90/90` with minimum margin `0.130416065`. The named helium-4 target
  remains reserved for prospective execution.

The supported commonality is O3 generation, interruption, physically specific
return, and history-dependent re-entry toward a domain-native whole. It is not
a common scalar mediator or cross-domain substance.

## Preserved boundaries and failures

- Fixed coordinate-radius and common absolute molecular thresholds are not
  representation invariant.
- Finite-seed point classification produced one false nonpass that disappeared
  under exact propagation.
- Molecular interruption-duration monotonicity had `76/2,030` local
  counterexamples.
- Scalar pre-return dislocation sufficiency was not supported
  (`1,097/2,030`, or `0.540394`).
- Instantaneous or adjacent-interval correct-return speed dominance was not
  supported universally (`1,527/12,180` molecular interval reversals).
- Return-rate dominance was not invariant across inequivalent readouts;
  Wasserstein geometry transport retained a molecular counterexample that did
  not appear under the registered distribution-identity metrics.
- The exploration does not establish a microscopic atom-to-cell law, a common
  O3 substance, living-cell confirmation, or universal numerical scaling.

A public experiment requires fresh targets, a public preregistration DOI, one
locked execution, complete preservation of supported and unsupported gates,
and a separate result release.

Its irreducibly cross-domain gate should be prospective leave-one-domain-out
transfer of stabilized O3-return specificity: the causal direction fixed from
two domains must predict every registered late checkpoint for a new target in
the third domain. Repeating three independent endpoint comparisons alone is not
sufficient.
