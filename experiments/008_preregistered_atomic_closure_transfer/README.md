# Experiment 008: Preregistered Atomic-Closure Transfer

Experiment 008 is the first public, preregistered transfer test of the
Subjectivity-Intersection Mathematics formation grammar in an atomic domain.

The registration freezes:

- the operational map from differentiated components to relational atomic
  closure;
- five structural bridge audits;
- two previously unused empirical holdouts;
- all thresholds and decision classes; and
- a runner that cannot access measurement values unless a post-registration
  measurement file is supplied explicitly.

The hypotheses were informed by unpublished exploratory computational work.
Those exploratory outputs are excluded from the confirmatory evidence.

Status: **PREREGISTERED — NOT EXECUTED**

Do not run the scientific execution mode before the registration commit and
tag are publicly verified. Registration validation is permitted:

```bash
python3 experiments/008_preregistered_atomic_closure_transfer/run.py \
  --validate-registration
python3 -m unittest \
  experiments/008_preregistered_atomic_closure_transfer/test_run.py
```

After registration, execution requires a separately prepared measurement file:

```bash
python3 experiments/008_preregistered_atomic_closure_transfer/run.py \
  --execute \
  --measurement-file /path/to/e008_measurements.json
```

Every generated output must be retained regardless of decision.
