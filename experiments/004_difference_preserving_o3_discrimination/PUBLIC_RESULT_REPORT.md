# Subjectivity-Intersection Mathematics Experiment 004

## Confirmatory Result Report

Status: CONFIRMATORY COMPLETE — REGISTERED CONJUNCTION SUPPORTED

## 1. Registration and execution provenance

- Registered commit: `ef2699aa126c78b2348381ee4ffc482accedd364`
- Preregistration tag: `e004-preregistration-v1.0.0`
- Preregistration DOI: <https://doi.org/10.5281/zenodo.21761975>
- E003R post-release correction DOI:
  <https://doi.org/10.5281/zenodo.21761925>
- Confirmatory descriptor indices: 128–255
- Confirmatory matrix seeds: 20261101–20261112
- Confirmatory cases: 1,536 pair-seed cases

The GitHub tag, Zenodo record, and execution checkout all resolved to the same
registered commit before the confirmatory command was run. The confirmatory
output directory did not exist before that execution.

## 2. Registered result

All four primary hypotheses passed at 128 of 128 content-unique pairs:

| Hypothesis | Passing pairs | Family 0 | Family 1 | Decision |
|---|---:|---:|---:|---|
| H1 difference preservation | 128/128 | 64/64 | 64/64 | Supported |
| H2 retained difference history | 128/128 | 64/64 | 64/64 | Supported |
| H3 O3 transport | 128/128 | 64/64 | 64/64 | Supported |
| H4 same-family pair specificity | 128/128 | 64/64 | 64/64 | Supported |

The complete registered conjunction passed.

All uniqueness firewalls also passed:

- unique complete A states: 128/128;
- unique complete B states: 128/128;
- unique complete A/B pairs: 128/128;
- unique four-encounter connector-packet sequences: 128/128;
- administrative identifiers in carrier input: none.

## 3. Architecture discrimination

Under the all-flip intervention, A+B and all downstream paths were fixed while
the orientation of A−B was reversed at joint generation.

- Candidate final-action distance: minimum `0.0774438749`, median
  `0.1361311023`.
- Symmetric recurrent final-action distance: exactly `0` in every case.
- Both architectures retained registered common-mode responsiveness.

The candidate therefore preserved information that was removed by the
sum-only symmetric architecture.

## 4. Retained difference history

The final current encounter was identical to baseline in the early-only and
prior-only interventions.

- Candidate early-only action distance: minimum `0.0209039690`, median
  `0.0447293089`.
- Candidate prior-only action distance: minimum `0.0541349146`, median
  `0.1068295959`.
- Memoryless early-only and prior-only distances: exactly `0` in every case.

The observed final effect therefore depended on retained differentiated
history rather than only on the current encounter.

## 5. O3 path and bilateral return

The all-flip effect passed the frozen thresholds at carrier, self-state, and
action. Erasing the self-state before the fixed action readout reduced the
action difference to exactly zero while leaving the already formed carrier
unchanged.

Completed C was then exchanged only between content-distinct pairs within the
same history family:

- A-return distance: minimum `0.1302506310`, median `0.2421121998`;
- B-return distance: minimum `0.1218791018`, median `0.2216452150`.

Both sides exceeded the registered threshold `0.07071548` in all 1,536
pair-seed cases.

## 6. What the result establishes

Experiment 004 establishes an executable relational architecture in which:

1. A and B remain differentiated rather than being reduced to their sum;
2. the orientation of that difference is carried through a recurrent C;
3. an earlier difference remains effective after the current input is matched;
4. the retained effect is transported from carrier to self-state to action;
5. replacing completed C within a matched history family changes the return to
   both A and B;
6. the frozen construction transfers to content-unique held-out subjectivity-
   agent states without changing its rules.

This is a formal and operational bridge between the Subjectivity-Intersection
framework and executable subjectivity-agent dynamics.

## 7. Architecture-class result

The role-aware recurrent control also passed the difference-preservation and
retained-history criteria at 128 of 128 pairs. The confirmed effect is
therefore not unique to the candidate multiplicative joint equation.

The supported object is a broader architecture class: distinct A/B routes,
relational recurrence, retained differentiated history, and a return path
through which the relational state changes both participants.

## 8. Interpretation boundary

The experiment confirms the registered computational construction and its
architecture-level discrimination. It provides a reproducible operational
model for C and self-reentrant O3 dynamics. Identification of these simulated
states with ontological subjectivity remains a separate philosophical and
empirical question.

## 9. Durable outputs

- `results/RESULT.md`
- `results/summary.json`
- `results/case_metrics.csv`
- `results/pair_decisions.csv`
- `results/output_manifest.json`

The output manifest verifies all generated result files by SHA-256.
