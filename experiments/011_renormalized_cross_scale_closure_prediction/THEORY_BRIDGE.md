# Experiment 011 theory bridge

## From repeated numbers to preserved relations

The integration exploration asked whether atomic, molecular, and cellular
third terms share a superficial number or a deeper closure relation. The raw
transition near `0.72` did not remain fixed under changes of representation.
The effective mediator coordinate did.

Subjectivity-Intersection Mathematics therefore makes the prospective claim
at the level of relation rather than raw coordinate:

```text
lower relational integrity -> represented molecular mediator -> cellular closure
lambda^2                  -> m=lambda^(2q)              -> survival transition.
```

The same constitutive relation can receive different raw coordinates under
different bridge representations. Representation covariance means that the
coordinate changes while the effective closure threshold is preserved.

## Public lineage

- E009 supplies the frozen reduced cellular dynamics, recovery endpoint, and
  life/death boundary.
- E010 supplies the public molecular standpoint that the complete relation,
  not isolated centres alone, constitutes the molecular whole.
- E011 tests a new cross-scale prediction generated after those two results:
  held-out raw thresholds are predicted from one frozen mediator threshold.

## Epistemic boundary

The equation `m=lambda^(2q)` is a frozen operational bridge law, not a derived
microscopic law of nature. A positive E011 result would show prospective
invariance under unexecuted representations inside this model. It would not
by itself show that natural atoms, molecules, and living cells are connected
by this numerical bridge.
